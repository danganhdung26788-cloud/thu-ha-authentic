{
  "name": "hermes-ai-gateway-dispatcher",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node --env-file=.env src/worker.js",
    "once": "node --env-file=.env src/worker.js --once",
    "dry-run": "node --env-file=.env src/worker.js --once --dry-run",
    "test": "node --test",
    "check": "node --check src/worker.js && node --check src/run-with-log-rotation.js"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  }
}
