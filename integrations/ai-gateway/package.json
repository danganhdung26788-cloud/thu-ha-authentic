{
  "name": "hermes-ai-gateway-dispatcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/worker.js",
    "once": "node src/worker.js --once",
    "dry-run": "node src/worker.js --once --dry-run",
    "test": "node --test"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  }
}
